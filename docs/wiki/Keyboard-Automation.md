# Keyboard Automation

Keyboard mode supports letters, digits, function keys, arrows, navigation
keys, modifiers, and special keys. Ctrl, Shift, Alt, and Win/Cmd can be used
with a selected key.

**Hold action for** keeps the key down for the selected duration. Use a small
interval carefully because the target application may become unresponsive.
