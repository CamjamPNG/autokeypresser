# Profiles and Sequences

Open **Profiles** to save the current action as a named profile. Use **Add
action** to append additional keyboard or mouse actions, then choose **Run
sequence** to replay them in order.

Profiles are stored in the user configuration directory and are separate from
`.akp` recorded macro files.
